# Do not persist query strings. OAuth codes, webhook verify tokens and signed
# callback parameters can be present in request arguments.
log_format bladeia_safe '$remote_addr - $remote_user [$time_local] '
                        '"$request_method $uri $server_protocol" $status $body_bytes_sent '
                        '"$http_referer" "$http_user_agent"';
